# 贡献指南

感谢你考虑为 ODM（正交领域建模）项目做出贡献！

---

## 如何贡献

### 报告问题

如果你发现了文档错误、建模规则不合理或有改进建议：

1. 在 Issues 中搜索是否已有相关问题
2. 如果没有，创建新 Issue，包含：
   - 清晰的标题
   - 问题描述
   - 复现场景（如适用）
   - 期望的改进

### 改进建模方法

ODM 是一个持续演进的方法论，欢迎贡献：

- **新案例**：在 `skills/m-modeling/references/demo.md` 中添加实际建模案例
- **规则优化**：对十条铁律提出改进或补充
- **域定义完善**：对六域归属提出更清晰的定义
- **复杂度测算改进**：优化建模复杂度的计算方法

### 改进文档

- 修正错别字、语句不通顺
- 补充示例说明
- 改进文档结构

---

## 贡献流程

### 1. Fork 并克隆

```bash
git clone https://github.com/YOUR_USERNAME/omd.git
cd omd
```

### 2. 创建分支

```bash
git checkout -b feature/your-feature-name
```

### 3. 进行修改

- 确保修改符合项目的建模理念
- 保持文档风格一致
- 如添加新概念，需在相应文档中说明

### 4. 提交变更

```bash
git add .
git commit -m "简明扼要的提交说明"
```

提交说明示例：
- `docs: 补充订单建模案例`
- `fix: 修正六域归属表中的示例`
- `feat: 添加新的复杂度测算维度`

### 5. 推送并创建 PR

```bash
git push origin feature/your-feature-name
```

然后在 GitHub 上创建 Pull Request。

---

## 文档规范

### 文件命名

- 使用小写字母和连字符：`new-guide.md`
- 编号文档保持格式：`1.guide.md`、`2.exceptions.md`

### 内容格式

- 使用简洁的中文表达
- 代码示例使用代码块包裹
- 表格用于展示规则、域归属等结构化内容
- 使用 `✅` 和 `❌` 标注正确/错误示例

### 建模案例格式

添加建模案例时，建议包含：

```markdown
## 案例：[场景名称]

**业务背景**：简述业务场景

**问题**：原有设计的问题

**分析**：
- 六域归属分析
- 违反的铁律

**改进方案**：
- 属性归属表
- 状态矩阵（如适用）

**效果**：改进后的优势
```

---

## 建模理念

贡献内容需符合 ODM 的核心理念：

1. **正交性**：不同维度的概念应独立，避免耦合
2. **可验证**：建模结果应能通过规则验证
3. **可量化**：复杂度应可测算，而非凭感觉
4. **可执行**：规则应足够明确，让 AI 也能执行

---

## 代码审查

所有 PR 都会经过审查，可能会提出修改建议：

- 请保持开放态度
- 及时响应审查意见
- 如有分歧，以建模理念为准进行讨论

---

## 许可证

提交贡献即表示你同意你的工作将按照本项目的许可证进行授权。

---

## 联系方式

如有疑问，可以：
- 在 Issues 中提问
- 在 Discussions 中讨论

再次感谢你的贡献！